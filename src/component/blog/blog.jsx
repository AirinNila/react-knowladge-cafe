import React from 'react';
import PropTypes from 'prop-types'

const Blog = ({blog}) => {

    return (
        <div>
            <div>
                {

                }
            </div>
        </div>
    );
};


Blog.propTypes = {
    Blog: PropTypes.object.isRequired
}
export default Blog;